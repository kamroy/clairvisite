# Uniquement nécessaire en production (déploiement séparé du serveur, sans proxy) :
# URL publique complète de l'API, terminée par /api. Laisser absent en dev — le proxy
# Vite (voir vite.config.js) se charge de router /api vers le serveur local.
VITE_API_URL="https://api.example.com/api"
